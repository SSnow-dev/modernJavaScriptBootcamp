alert("It's Working!");

console.log("All is good!");

const square = n1 => n1 * n1;

const isEven = n1 => n1 % 2 === 0 ? true : false;

let str = "Hello World!";

for (let char of str) {
  console.log(char);
}